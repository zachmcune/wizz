# Optional: WebSocket URL for the lockstep relay (production deploy).
# Local dev defaults to ws://<hostname>:8787 when unset.
# Set in Cloudflare Pages env vars (build time), not on the relay host.
# See docs/RELAY_DEPLOY.md for Railway setup.
# VITE_RELAY_URL=wss://your-relay.up.railway.app
